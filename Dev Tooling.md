- direnv
- neovim + custom config
- tmux + custom config
- zsh  + custom config
- git
- rlwrap
- 

## `direnv`

Shell extension that simplifies managing environment variables

```sh
sudo apt install direnv
#todo: add shell hook
# eval "$(direnv hook zsh)"
```


## `neovim`

Go vimasterrace doode.

```
sudo apt install neovim
```

#todo pull custom config and fix all the damn dependencies


## `tmux`

```sh
sudo apt install tmux
```

#todo pull custom config

## `zsh`

```sh
sudo apt install zsh
```

#todo: make default shell (zsh), add custom .zshrc
## `shellgpt`

Requires python n shit
